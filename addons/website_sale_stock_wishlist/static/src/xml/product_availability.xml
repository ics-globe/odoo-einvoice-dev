<?xml version="1.0" encoding="UTF-8"?>

<templates>
    <t t-name="website_sale_stock_wishlist.product_availability" inherit_id="website_sale_stock.product_availability">
        <div id="stock_wishlist_message" t-if="product_type == 'product' and !free_qty and !allow_out_of_stock_order"
             t-attf-class="availability_message_#{product_template} d-flex align-items-center flex-column flex-md-row">
            <button
                    id="save_for_later_button"
                    t-if="!wish"
                    type="button"
                    role="button"
                    class="btn btn-secondary text-nowrap o_add_wishlist_dyn"
                    t-att-data-product-template-id="product_id"
                    t-att-data-product-product-id="product_id"
                    data-action="o_wishlist"
                    title="Add to wishlist">
                <i class="fa fa-clock-o  mr-1"/>
                Save for later
            </button>
            <div id="added_to_your_wishlist_alert" t-att-class="wish ? '' : 'd-none'">
                <div class="alert alert-success">
                    <i class="fa fa-heart mr-1"/>
                    Added to your wishlist
                </div>

            </div>

        </div>
    </t>
</templates>
