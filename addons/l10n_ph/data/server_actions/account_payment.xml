<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_account_payment_bir_2307" model="ir.actions.server">
        <field name="name">Download BIR 2307 XLS</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="binding_model_id" ref="account.model_account_payment"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if record.payment_type == 'outbound':
    wizard_action = env["ir.actions.act_window"]._for_xml_id("l10n_ph.view_l10n_ph_2307_wizard_act_window")
    wizard_action.update({
        'context': {'default_moves_to_export': record.reconciled_bill_ids.ids}
    })
    action = wizard_action
else:
    raise UserError('Only Outbound Payment is available.')
]]></field>
    </record>
</odoo>
